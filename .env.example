VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Set to true after deploying this app to a custom Netlify domain. The default
# Netlify subdomain uses the same-origin /supabase proxy automatically.
VITE_USE_SUPABASE_PROXY=
